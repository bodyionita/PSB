# --- Python / server ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.*
!.env.example
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

# --- Node / web ---
node_modules/
dist/
dist-ssr/
*.local
.vite/

# --- deploy / runtime secrets & data ---
deploy/.env
deploy/secrets/
deploy/origin.crt          # CI-rendered origin TLS cert (ADR-017); never committed
deploy/origin.key          # CI-rendered origin TLS key  (ADR-017); also caught by *.key
srv/
/data/
# local dev graph store (default GRAPH_STORE_PATH=../graph-store)
/graph-store/
*.log

# --- secrets & key material (never commit; enforced by .githooks/pre-commit) ---
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_dsa
id_ecdsa
id_ed25519
!*.pub

# --- editors / OS ---
.DS_Store
.idea/
.vscode/
Thumbs.db
